{{>licenseInfo}}
{{#operations}}/*
* {{classname}}.h
*/
#ifndef {{classname}}_H_
#define {{classname}}_H_

{{{defaultInclude}}}
#include <drogon/HttpController.h>
#include "{{classname}}Interface.h"
#include <functional>
#include <memory>
#include <string>

{{#imports}}{{{import}}}
{{/imports}}

namespace {{apiNamespace}} {

class {{declspec}} {{classname}} : public drogon::HttpController<{{classname}}, false> {
public:
    /* ---------- Route table ---------- */
    METHOD_LIST_BEGIN
{{#operation}}
    // Register route; path params are positional placeholders {1}, {2}, ...
    ADD_METHOD_TO({{classname}}::{{vendorExtensions.operationIdSnakeCase}}_handler,
                  base + "{{{vendorExtensions.x-codegen-drogon-path}}}",
                  drogon::{{httpMethod}});
{{/operation}}
    METHOD_LIST_END

    /* ---------- Handler prototypes ---------- */
{{#operation}}
    void {{vendorExtensions.operationIdSnakeCase}}_handler(
        const drogon::HttpRequestPtr& req,
        std::function<void (const drogon::HttpResponsePtr &)> &&callback
        {{#pathParams}}, const {{{dataType}}}& path_{{paramName}}{{/pathParams}}
    );
{{/operation}}

    /* Fallback for unmatched routes */
    void {{classnameSnakeLowerCase}}_default_handler(
        const drogon::HttpRequestPtr& req,
        std::function<void (const drogon::HttpResponsePtr &)> &&callback);

    /* Global prefix (e.g. "/v2") – defined in .cpp */
    static const std::string base;

    {{classname}}() = default;
    explicit {{classname}}(std::shared_ptr<{{classname}}Interface> svc)
        : service(std::move(svc)) {}

private:
    std::shared_ptr<{{classname}}Interface> service;

    // Error helpers overridable by users if they subclass
    virtual void handleParsingException(
        const std::exception& ex,
        std::function<void (const drogon::HttpResponsePtr &)> &&callback) const noexcept;

    virtual std::pair<drogon::HttpStatusCode, std::string>
    handleParsingException(const std::exception& ex) const noexcept;

    virtual void handleOperationException(
        const std::exception& ex,
        std::function<void (const drogon::HttpResponsePtr &)> &&callback) const noexcept;

    virtual std::pair<drogon::HttpStatusCode, std::string>
    handleOperationException(const std::exception& ex) const noexcept;
};

} // namespace {{apiNamespace}}

#endif /* {{classname}}_H_ */
{{/operations}}
