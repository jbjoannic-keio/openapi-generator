{{>licenseInfo}}
{{#operations}}/*
* {{classname}}Interface.h
*/
#ifndef {{classnameSnakeUpperCase}}_INTERFACE_H_
#define {{classnameSnakeUpperCase}}_INTERFACE_H_

{{{defaultInclude}}}
#include <drogon/HttpTypes.h>
#include <optional>
    #include <utility>
    #include <variant>
    #include <stdexcept>
    #include <tuple>   // <-- needed if you enable CT in the variant
    {{#imports}}{{{import}}}{{/imports}}

    namespace {{apiNamespace}} {

    class {{declspec}} {{classname}}Interface {
    public:
    virtual ~{{classname}}Interface() = default;

    /* ---------- response variants ---------- */
    {{#operation}}
        using {{operationId}}Response = std::variant<
        {{#vendorExtensions.x-codegen-response-variants}}
            {{#contentInfos}}{{^isVariantDuplicated}}
                {{#dataType}}
                    // Minimal: status + body (your current approach)
                    // std::pair<std::integral_constant<drogon::HttpStatusCode, drogon::{{statusCode}}>, {{{dataType}}}}>
                    // Recommended: status + content-type + body (uncomment below and comment the line above)
                    std::tuple<
                    std::integral_constant<drogon::HttpStatusCode, drogon::{{statusCode}}>,
                    std::integral_constant<drogon::ContentType, drogon::{{contentType}}>,
                    {{{dataType}}}
                    >
                    {{/dataType}}
                    {{^dataType}}
                    std::integral_constant<drogon::HttpStatusCode, drogon::{{statusCode}}>
                    {{/dataType}}{{^-last}},{{/-last}}
                    {{/isVariantDuplicated}}{{/contentInfos}}
                    {{/vendorExtensions.x-codegen-response-variants}}
                    >;
                    {{/operation}}

                    /* ---------- abstract operations ---------- */
                    {{#operation}}
                    /// <summary>{{summary}}</summary>
                    {{#notes}}/// <remarks>{{notes}}</remarks>{{/notes}}

                    {{#vendorExtensions.x-codegen-drogon-is-parsing-supported}}
                    virtual void {{vendorExtensions.operationIdSnakeCase}}(
                    {{#allParams}}
                    const {{#isModel}}{{^isOptional}}{{modelNamespace}}::{{/isOptional}}{{/isModel}}{{{dataType}}}& {{#isHeaderParam}}header_{{/isHeaderParam}}{{#isQueryParam}}query_{{/isQueryParam}}{{#isPathParam}}path_{{/isPathParam}}{{paramName}}{{^-last}}, {{/-last}}
                    {{/allParams}}{{#hasParams}}, {{/hasParams}}
                    {{operationId}}Response& response) = 0;
                    {{/vendorExtensions.x-codegen-drogon-is-parsing-supported}}


                    {{^vendorExtensions.x-codegen-drogon-is-parsing-supported}}
                    virtual void {{vendorExtensions.operationIdSnakeCase}}(
                    const drogon::HttpRequestPtr& req,
                    std::function<void (const drogon::HttpResponsePtr &)> &&callback
                    {{#pathParams}}, const {{{dataType}}}& path_{{paramName}}{{/pathParams}},
                    {{operationId}}Response& response) = 0;
                    {{/vendorExtensions.x-codegen-drogon-is-parsing-supported}}

                    {{/operation}}
                    };

                    } // namespace {{apiNamespace}}
                    #endif /* {{classnameSnakeUpperCase}}_INTERFACE_H_ */
                    {{/operations}}